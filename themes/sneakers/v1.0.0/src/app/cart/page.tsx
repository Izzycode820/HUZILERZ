import { CartView } from '@/components/cart/CartView';

export const metadata = {
  title: 'Shopping Cart | Sneakers',
  description: 'Review your items and proceed to checkout.',
};

export default function CartPage() {
  return <CartView />;
}
