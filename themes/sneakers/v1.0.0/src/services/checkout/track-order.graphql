query TrackOrder(
  $orderNumber: String!
  $phone: String!
) {
  trackOrder(
    orderNumber: $orderNumber
    phone: $phone
  ) {
    orderNumber
    status
    totalAmount
    createdAt
    trackingNumber
  }
}