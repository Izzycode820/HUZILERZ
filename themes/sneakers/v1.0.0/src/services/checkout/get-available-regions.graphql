query GetAvailableRegions($sessionId: String!) {
  availableShippingRegions(sessionId: $sessionId) {
    success
    regions {
      name
      price
      estimatedDays
    }
    message
    error
  }
}
