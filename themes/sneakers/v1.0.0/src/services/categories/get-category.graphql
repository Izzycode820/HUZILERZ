query GetCategory($categorySlug: String!) {
  category(categorySlug: $categorySlug) {
    id
    name
    slug
    description
    categoryImage {
      optimizedUrl
      thumbnailUrl
    }
    productCount
  }
}