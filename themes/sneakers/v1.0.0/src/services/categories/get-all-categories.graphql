query GetAllCategories($first: Int = 50) {
  categories(first: $first) {
    edges {
      node {
        id
        name
        slug
        categoryImage {
          thumbnailUrl
          optimizedUrl
        }
        productCount
        isFeatured
      }
    }
  }
}