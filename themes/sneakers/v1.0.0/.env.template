# GraphQL API Endpoint
NEXT_PUBLIC_GRAPHQL_URI=http://localhost:8000/api/graphql

# Root Domain for Store Subdomains
# Used to construct the store hostname in development (e.g. store-slug.huzilerz.com)
# and to determine the store slug in production.
NEXT_PUBLIC_ROOT_DOMAIN=huzilerz.com
