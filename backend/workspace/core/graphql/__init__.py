"""
GraphQL types and utilities for workspace core

This module contains shared GraphQL types used across all workspace types:
- Customer types (used by store, blog, service workspaces)
- Workspace types
- Common types (pagination, errors, etc.)
"""
